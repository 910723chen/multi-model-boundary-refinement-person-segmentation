# Model Weights

Model weights are not included in this GitHub release because they are large files.

Before running the full pipeline, place the following files in this folder:

```text
sam_vit_b_01ec64.pth
yolov8n.pt
yolov8n-seg.pt
```

Do not commit these files directly to GitHub. The SAM checkpoint exceeds GitHub's normal single-file size limit.
